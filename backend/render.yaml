# Render.com Blueprint
# Deploy from GitHub

services:
  - type: web
    name: edgeai-blockchain
    env: docker
    dockerfilePath: ./Dockerfile
    region: singapore
    plan: starter
    healthCheckPath: /api/chain
    envVars:
      - key: RUST_LOG
        value: info
      - key: PORT
        value: 8080
